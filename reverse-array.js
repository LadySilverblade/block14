// With a for loop, create a JavaScript code that creates a new array in reverse order.
const countdown = [20, 50, 30, 60, 200];
for (let i = countdown.length - 1; i >= 0; i--) {
	console.log(countdown[i]);
}
